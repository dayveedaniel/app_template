import 'dart:ui' show Locale;

const supportedLocales = [Locale('en')];

const duration_100ms = Duration(milliseconds: 100);
const duration_200ms = Duration(milliseconds: 200);
const duration_300ms = Duration(milliseconds: 300);
const duration_400ms = Duration(milliseconds: 400);
const duration_500ms = Duration(milliseconds: 500);

const duration_1s = Duration(milliseconds: 1000);
const duration_3s = Duration(milliseconds: 3000);
const duration_10s = Duration(milliseconds: 10000);

const double inactiveOpacity = 0.5;

const String naText = 'N/A';
